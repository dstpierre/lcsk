(() => {
  // no jquery
  // bootstrap

  // Inject some kind CSS without <link>
  // Widget needs to tell if we are in-app [user signed in] | website visitor
  // Create the UI
  // inline html / createElement / manual JS event
  // User|Visitor engage, view older discussion
  // Be able to see who is online
})();